---
title: 'The Lorentz factor'
category: foundations
layoutArchetype: distribution
status: live
order: 0
equationLatex: '\gamma = \frac{1}{\sqrt{1 - v^2/c^2}}'
equationPlain: 'γ = 1 / √(1 − v²/c²)'
claim: 'One number appears throughout special relativity, though different quantities use γ or its reciprocal. Drag the speed and watch γ climb.'
defaults:
  beta: 0.6
terms:
  - symbol: '\gamma'
    name: 'Gamma: Greek lowercase gamma'
    role: 'the factor relating measurements of time, longitudinal length, momentum, and energy between inertial frames'
    effect: 'always at least 1; this is the number that grows as you approach light speed'
  - symbol: 'v'
    name: 'v: Latin letter vee, the speed'
    role: 'how fast something moves relative to you'
    effect: 'the closer to c, the bigger γ becomes'
  - symbol: 'c'
    name: 'c: the speed of light in vacuum, 299,792,458 m/s'
    role: 'the cosmic speed limit that sets the scale'
    effect: 'only the ratio v/c matters, never v on its own'
  - symbol: 'v^2/c^2'
    name: 'v squared over c squared: the speed fraction, squared'
    role: 'how close to light speed you are'
    effect: 'climbs toward 1 as v approaches c'
  - symbol: '\sqrt{1 - v^2/c^2}'
    name: 'the square root in the denominator'
    role: 'shrinks from 1 toward 0 as v approaches c'
    effect: 'as this shrinks, one divided by it (γ) blows up'
presets:
  - label: 'Half light speed'
    caption: 'γ ≈ 1.15: time and energy factors are about 15% above their rest values, while longitudinal length is about 87% of its rest length.'
    values:
      beta: 0.5
  - label: 'γ = 2'
    caption: 'At 0.866 c, γ is exactly 2: a moving clock advances half as much, lengths parallel to the motion halve, and transverse lengths do not contract.'
    values:
      beta: 0.866
  - label: 'γ ≈ 7'
    caption: 'At 0.99 c, γ is about 7. The last sliver of speed is where γ explodes.'
    values:
      beta: 0.99
limits:
  - label: 'v → 0'
    icon: '🐢'
    collapse: 'γ → 1: relativity switches off and Newton takes over'
    values:
      beta: 0
  - label: 'v → c'
    icon: '⚡'
    collapse: 'γ → ∞ for time and energy, while the longitudinal length factor 1/γ → 0; transverse lengths do not contract'
    values:
      beta: 0.999
prerequisites: []
related:
  - relativity/time-dilation
  - relativity/length-contraction
  - relativity/mass-energy
history:
  year: '1892-1905'
  people:
    - Hendrik Lorentz
    - Henri Poincaré
    - Albert Einstein
  summary:
    - >-
      The factor is named after Hendrik Lorentz, who in a series of papers from
      1892 to 1904 wrote down the transformations that carry his name to explain
      why experiments kept failing to detect the Earth's motion through the
      supposed ether. The shrinking factor first showed up as the longitudinal
      FitzGerald-Lorentz length contraction, and Woldemar Voigt had already
      written similar equations in 1887 while studying the Doppler effect.
    - >-
      In 1905 Henri Poincaré tidied the equations into a group and named them
      the Lorentz transformation, and Albert Einstein's special relativity gave
      γ its modern meaning: not an ether effect but a real property of space and
      time once the speed of light is the same for every observer. The same
      factor now sets the clock corrections in GPS and the energies reached in
      particle accelerators.
  references:
    - label: 'Wikipedia: Lorentz factor'
      url: https://en.wikipedia.org/wiki/Lorentz_factor
    - label: 'Wikipedia: Lorentz transformation'
      url: https://en.wikipedia.org/wiki/Lorentz_transformation
---

import GammaCurve from '../../../components/viz/GammaCurve.astro';
import SliderField from '../../../components/SliderField.astro';
import PresetButtons from '../../../components/PresetButtons.astro';
import LimitSnaps from '../../../components/LimitSnaps.astro';
import ResetButton from '../../../components/ResetButton.astro';

<GammaCurve slug="foundations/lorentz-factor" />

<div class="viz-controls">
  <SliderField
    label="Speed"
    unit="c"
    slug="foundations/lorentz-factor"
    storeKey="beta"
    min={0}
    max={0.999}
    scale="beta"
    sigFigs={3}
    value={frontmatter.defaults.beta}
  />
  <PresetButtons slug="foundations/lorentz-factor" presets={frontmatter.presets} />
  <LimitSnaps slug="foundations/lorentz-factor" limits={frontmatter.limits} />
  <ResetButton slug="foundations/lorentz-factor" label="Reset to 0.6 c" />
</div>

## One factor behind many relationships

The Lorentz factor γ appears throughout the equations that replace Newtonian
physics at high speeds, but it is not a universal multiplier. It is built from
just one ingredient: how fast you are going as a fraction of the speed of light.
Square that fraction, subtract it from 1, take the square root, and flip it over.
That is the whole recipe.

The shape of the curve is the point. For everyday speeds, even a fast jet, v/c
is so tiny that γ sits at 1.000 and relativity is invisible. Push past about
half the speed of light and γ starts to climb. In the last few percent before c
it shoots up without limit, because the square root in the denominator is racing
toward zero.

## Where γ shows up

Different relativistic relationships use γ in different ways:

- **Time dilation** — a moving clock's ticks stretch by γ. See it move on the{' '}
  <a href={`${import.meta.env.BASE_URL}viz/relativity/time-dilation/`}>time dilation page</a>.
- **Length contraction** — only the object's length parallel to its direction of
  motion shortens by a factor of 1/γ; transverse lengths do not contract. See it on the{' '}
  <a href={`${import.meta.env.BASE_URL}viz/relativity/length-contraction/`}>length contraction page</a>.
- **Energy** — a moving mass carries energy γmc², which is why reaching c would
  take infinite energy. See it on the{' '}
  <a href={`${import.meta.env.BASE_URL}viz/relativity/mass-energy/`}>mass–energy page</a>.

<details class="misconception">
  <summary>Can γ ever be less than 1?</summary>

  No. γ is always at least 1, and equals 1 only when the object is at rest.
  Motion can stretch time intervals and shrink length parallel to the relative
  motion, but transverse lengths do not contract. From your point of view a
  moving clock never ticks *faster*, and a ruler aligned with the motion never
  grows *longer*. The square root is always between 0 and 1, so one divided by
  it is always 1 or more.
</details>
