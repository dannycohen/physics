---
// The shared opening for every landing variant: the site title + intro
// paragraph, defined once here so the variant pages reuse the same copy instead
// of duplicating it. Each page renders <LandingIntro /> above its switcher; the
// layout below the switcher stays the page's own.
---

<h1>Grab a slider. Bend the universe.</h1>
<p>
  Every page here is one famous equation of physics turned into a toy. Drag a slider,
  watch clocks slow down and curves shoot up, and find out why the equation has the
  shape it does. No prior physics needed, just curiosity.
</p>
